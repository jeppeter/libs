
#include "dllex.h"
#include <stdio.h>
#include <stdlib.h>
int PrintFunc(const char* pName)
{
	printf("PrintFunc: %s\n",pName);
	return 0;
}

int RepeatFunc(const char* pName)
{
	printf("RepeatFunc: %s\n",pName);
	return 0;
}